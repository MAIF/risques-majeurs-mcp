---
sidebar_position: 6
title: Contribuer
description: Comment contribuer au projet Risques Majeurs MCP
---

# Contribuer

Merci de votre interet pour Risques Majeurs MCP ! Toute contribution est la bienvenue.

## Prerequis

- [Node.js](https://nodejs.org/) >= 24 (ou [mise](https://mise.jdx.dev/))
- [Git](https://git-scm.com/)

## Mise en place

```bash
# Cloner le depot
git clone https://github.com/MAIF/risques-majeurs-mcp.git
cd risques-majeurs-mcp

# Installer les dependances
npm install

# Lancer en mode developpement
npm run start-dev
```

Ou avec mise :

```bash
mise run dev
```

## Lancer les tests

```bash
npm test
```

Ou avec mise :

```bash
mise run test
```

## Taches mise disponibles

| Tache | Description |
|---|---|
| `mise run install` | Installer les dependances |
| `mise run build` | Build serveur + client |
| `mise run dev` | Lancer en mode developpement |
| `mise run test` | Lancer les tests |
| `mise run start` | Lancer en mode production |
| `mise run docs:dev` | Lancer la documentation en mode dev |
| `mise run docs:build` | Builder la documentation |
| `mise run release -- X.Y.Z` | Preparer une release |

## Ajouter un nouveau risque

Pour ajouter un nouveau risque, ajoutez une entree dans le tableau `RISQUES` du fichier `server/risques.ts`. Chaque risque doit definir :

```typescript
{
  code: 'mon_risque',                    // Identifiant unique
  libelle: 'Mon risque',                 // Nom affiche
  fetch: async (longitude, latitude) => {
    // Appel a l'API Georisques (aiguillage vers l'endpoint v1 ou v2 selon la configuration du jeton d'authentification)
    // Retourne les donnees brutes
  },
  outputSchema: z.object({...}),         // Schema Zod
  text: (exposition) => {
    // Retourne un resume texte
  },
  layers: [                              // Couches carte (ou [] si pas de carte)
    {
      id: 'mon_risque',
      nom: 'Mon risque',
      source: (exposition) => ({...}),   // Source MapLibre
      layer: {...},                      // Config couche MapLibre (tableau accepte pour appliquer differents styles)
      legend: (exposition) => {...},     // Element DOM de legende
    }
  ]
}
```

Les outils `exposition_risques` et `carte_exposition_risques` prendront automatiquement en charge le nouveau risque.

## Soumettre une contribution

1. Forkez le depot
2. Creez une branche depuis `main`
3. Faites vos modifications
4. Verifiez que le build passe : `npm run build`
5. Verifiez que les tests passent : `npm test`
6. Ouvrez une Pull Request en utilisant le [template PR](https://github.com/MAIF/risques-majeurs-mcp/blob/main/.github/PULL_REQUEST_TEMPLATE.md)

## Signaler un bug ou proposer une idee

Ouvrez une [issue sur GitHub](https://github.com/MAIF/risques-majeurs-mcp/issues) en utilisant le template fourni.

## Code de conduite

Ce projet suit un [Code de conduite](https://github.com/MAIF/risques-majeurs-mcp/blob/main/.github/CODE_OF_CONDUCT.md). En participant, vous vous engagez a le respecter.

## Licence

En contribuant, vous acceptez que vos contributions soient distribuees sous licence [Apache 2.0](https://www.apache.org/licenses/LICENSE-2.0).
