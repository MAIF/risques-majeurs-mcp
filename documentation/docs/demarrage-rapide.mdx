---
sidebar_position: 2
title: Demarrage rapide
description: Installer et lancer le serveur Risques Majeurs MCP en quelques minutes
---

# Demarrage rapide

## Prerequisites

- [Node.js](https://nodejs.org/) >= 24 (ou utilisez [mise](https://mise.jdx.dev/) pour installer la bonne version automatiquement)

## Installation

```bash
npm install
```

Ou avec mise :

```bash
mise run install
```

## Builder le projet

```bash
npm run build
```

Ou avec mise :

```bash
mise run build
```

Le build genere `dist/index.js` (serveur) et `dist/client/mcp-app.html` (carte).

## Configurer un client MCP

Le moyen le plus simple est de laisser le client MCP **lancer le serveur lui-meme via stdio** : pas de serveur a demarrer en parallele, pas de port a ouvrir.

### Avec stdio (recommande)

Ajoutez le serveur dans la configuration de votre client MCP, en pointant vers le fichier `dist/index.js` avec le flag `--stdio` :

```json
{
  "mcpServers": {
    "risques-majeurs": {
      "command": "node",
      "args": ["/chemin/absolu/vers/risques-majeurs-mcp/dist/index.js", "--stdio"]
    }
  }
}
```

Ce format est accepte par **Claude Desktop**, **Gemini CLI**, **Codex CLI** (en TOML), et la plupart des clients MCP.

Pour **Claude Code** :

```bash
claude mcp add risques-majeurs -- node /chemin/absolu/vers/risques-majeurs-mcp/dist/index.js --stdio
```

### Avec HTTP (alternative)

Si vous preferez lancer le serveur a la main (utile en developpement avec hot-reload, ou pour exposer le serveur a plusieurs clients), demarrez-le d'abord :

```bash
npm run start-dev    # dev avec hot-reload
# ou
npm start            # production
```

Le serveur ecoute sur `http://localhost:3000/mcp`. Configurez ensuite le client :

```json
{
  "mcpServers": {
    "risques-majeurs": {
      "url": "http://localhost:3000/mcp"
    }
  }
}
```

Pour **Claude Code** :

```bash
claude mcp add risques-majeurs http://localhost:3000/mcp --transport http
```

### Plus de details

La page [**Connecter un client IA**](./clients-mcp) detaille la configuration des deux modes pour chaque plateforme : Claude Desktop, Claude Code, ChatGPT, Codex CLI, Mistral Le Chat, Gemini CLI, Cursor, Cline, Continue, Zed…

## Verifier que ca fonctionne

Une fois connecte, votre assistant IA devrait avoir acces a 4 nouveaux outils :

- **geocodage** — Geocoder une adresse
- **liste_risques** — Lister les risques disponibles
- **exposition_risques** — Evaluer l'exposition aux risques
- **carte_exposition_risques** — Afficher une carte des risques

Essayez par exemple de demander a votre assistant :

> *"Quels sont les risques majeurs au 1 place de la Comedie a Montpellier ?"*

## Variable d'environnement

| Variable | Description | Defaut |
|---|---|---|
| `PORT` | Port d'ecoute du serveur HTTP | `3000` |
| `NODE_ENV` | `development` pour ecouter sur `127.0.0.1`, sinon `0.0.0.0` | — |
| `CORS_ORIGIN` | Origine autorisee pour les requetes CORS | `*` |
| `RATE_LIMIT_WINDOW_MS` | Fenetre de temps du rate limiting (en ms) | `60000` |
| `RATE_LIMIT_MAX` | Nombre max de requetes par fenetre par IP | `100` |
| `API_V2_TOKEN` | Jeton d'authentification Géorisques (si configuré, les endpoints V2 sont utilisés) | — |
